settings:
  # Add rules whose output will be treated as temporary and removed
  # once depending rules have completed
  temp_rules: 
    - picard_add_or_replace_read_groups
    - picard_sort_bam
  #   - sratools_prefetch
  #   - sratools_fastq_dump
  #   - bowtie_align
bio.ngs.settings:
  sample_organization: sample_run_sra
  sample_column_map:
    SampleName: SM
    Run: PU
  db:
    ref: $BIODATA_HOME/genomes/Hsapiens/hg19/seq/hg19.fa
  read_length: 50
